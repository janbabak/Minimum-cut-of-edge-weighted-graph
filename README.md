# Translate to English: minimum cut of an edge-weighted graph

```bash
docker build -t minimum-cut-img .
```

```bash
docker run -it -v `pwd`:/work --name minimum-cut minimum-cut-img
```

```bash
make run
```